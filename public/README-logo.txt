Drop the Own It Social logo here as "logo.png".
It renders at 36px tall in the top navigation bar (transparent background
recommended, since the bar is dark navy). If the file is missing, the nav
simply shows the "Agent Portal" text without a logo.
