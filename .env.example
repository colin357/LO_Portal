# Copy this file to .env and fill in your Firebase project's web app config.
# Firebase console -> Project settings -> General -> Your apps -> SDK setup and configuration
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Optional: AI Assistant LLM endpoint. If set, the assistant POSTs
# { question, resources } here and expects { text, resources? } back.
# Leave blank to use the built-in, offline knowledge engine (no API key needed).
VITE_ASSISTANT_ENDPOINT=
